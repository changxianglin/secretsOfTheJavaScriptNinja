/*
* 9.1 数组
* 数组是最常见的数据类型之一。
*
* 9.1.1 创建数组
* 使用内置的 Array 构建函数
* 使用数组字面量[]
*
* JavaScript 的数组对象，假如访问不存在的对象，会返回 undefined。
* JavaScript 可以手动修改 length 属性的值。
*
* 9.1.2 在数组两端添加、删除元素。
* push
* unshift
* pop
* shift
*
*
* */